/**
 * Both BASE_SCREEN_DESIGN_WIDTH and BASE_SCREEN_DESIGN_HEIGHT are the
 * base reference width and height used while preparing the ui-design of
 * the application in Figma.
 *
 * NOTE: Changing this could lead to un intended consequences throughout the layout
 * and dimensions of the entire application. Kindly never change the value of
 * these constants unless you're fully aware of what you're doing.
 *
 * FIXME: change this to actual value when the design is ready.
 */
export const BASE_SCREEN_DESIGN_WIDTH = 0;
export const BASE_SCREEN_DESIGN_HEIGHT = 0;
