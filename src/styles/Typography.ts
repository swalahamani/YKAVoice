import {} from "@utils/ResponsiveScreenUtil";

/**
 * All different fontFamilies used throughout the application
 * should be defined here.
 */
const fonts = Object.freeze({});

/**
 * All different fontSizes used throughout the application
 * should be defined here.
 */
const fontSizes = Object.freeze({});

export default {fonts, fontSizes};
